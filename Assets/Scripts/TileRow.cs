using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileRow : MonoBehaviour
{
    public TileCell[] cells;

    private void Awake()
    {
        //for (int i = 0; i < 4; i++)
        //{
            //cells[i] = GetComponentInChildren<TileCell>();
        //}

        //cells=GetComponentInChildren<TileCell>();
    }
}
